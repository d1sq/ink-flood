.idea/
node_modules/
dist/
.release/
.env
*.local
*.zip

# Built compendium pack (LevelDB) — sources live in data/compendium/, rebuild via npm run build:packs.
# Existing packs/actors/ is the legacy hand-built pack and IS committed.
packs/ink-creatures/
